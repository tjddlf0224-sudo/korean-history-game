# 대사 개편 규격 — 선사 1화에서 확정한 방식

선사 1화(`seonsa1.html`)를 이 방식으로 고쳤고, 사용자가 방향을 승인했다.
나머지 33개 챕터를 같은 규격으로 옮긴다.

## 0. 왜 고치나

재 보니 대사 1148개 중 **92%가 글자뿐**이었다(도표 4%·사진 4.4%). 평균 62자,
30%가 70자를 넘었고 가장 긴 것은 171자였다. 열 챕터는 시각 자료가 하나도 없었다.

**학습자는 문제를 틀려서 못 푸는 게 아니라, 안 읽어서 못 푼다.**

## 1. 세 가지 원칙

### ① 짧게 — 한 마디에 한 가지만

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 평균 | 70.7자 | **23.2자** |
| 가장 긴 것 | 90자 | 37자 |
| 대사 수 | 35개 | 122개 |

목표는 **평균 25~35자, 최대 40자**. 한 마디에 사실을 두 개 이상 넣지 않는다.
길이가 줄어드는 대신 수가 는다 — 총량은 비슷하거나 조금 준다.

### ② 시켜 보고 가르친다 — NPC 독백 금지

`who:'me'`로 주인공이 말할 수 있다(렌더러가 이름 '나'·얼굴·초록 테두리로 바꾼다).

**나쁜 예** (지식을 읊는다)
```js
{ who:'npc', t:'우리는 강가에 자리를 잡았네. 움집을 파고 여럿이 모여 살지. 조와 피를 심어 거두고, 그물로 고기도 잡네.' },
```

**좋은 예** (시키고, 해 보고, 알게 된다)
```js
{ who:'npc', t:'거기 그릇 좀 세워 주게. 손이 모자라서.' },
{ who:'me',  t:'(그릇을 바닥에 놓는다) …어? 자꾸 넘어지는데요.' },
{ who:'npc', t:'(웃으며) 바닥에 세우려 하니 그렇지. 모래에 꽂게.' },
{ who:'me',  t:'(모래에 꽂는다) …아, 밑이 뾰족하군요.', chart:{ ... } },
```

- **괄호 지문**으로 숨을 준다: `(웃으며)`, `(한숨)`, `(검을 툭 치며)`, `(잠시 말이 없다)`
- 주인공 대사 비율 **30~40%**가 적당하다(선사 1화는 36%)
- NPC마다 말투를 달리한다(사냥꾼은 반말, 부여 사람은 `~하오`체 등)
- 상황을 만든다: 일을 시킨다 / 물건을 쥐어 준다 / 재판을 보여 준다 /
  주인공이 실수한다(동예에서 냇물을 넘어 책화를 배운다)

### ③ 시각 자료를 최대한 붙인다

**도해 비율 30~40%**를 목표로 한다(선사 1화는 42개/122대사 = 34%).
말로 설명하려는 것을 그림으로 옮길 수 있으면 옮긴다.

- '어디'를 말하면 → `map`
- '어떻게 만드나'를 말하면 → `steps`
- '무엇이 다른가'를 말하면 → `compare` 또는 `vs`
- 여럿을 견주면 → `grid`
- 옛 글을 인용하면 → `quote`
- 수를 말하면 → `stat`
- 유물·인물을 가리키면 → `relic`·`faces` (이미 있는 그림을 쓴다)

## 2. 도해 규격 (`assets/chart.js` · 16종)

모든 도해에 **`title`**을 단다. 제목 없이 그림만 뜨면 뜬금없다.
제목은 **시험에 나오는 말**로 짓는다 — 문제에서 그 말을 보면 그림이 떠오르게.

```js
chart:{ type:'timeline', title:'고조선 2000년', marks:[
  { y:'BC 2333', t:'고조선' }, { y:'BC 108', t:'멸망', on:true } ] }

chart:{ type:'compare', title:'구석기와 신석기', cols:[
  { h:'구석기', items:['떠돌이','뗀석기'] },
  { h:'신석기', hi:true, items:['정착','간석기'] } ] }

chart:{ type:'relic', title:'구석기의 도구', items:[
  { img:'jumeokdokki', n:'주먹도끼', s:'깨뜨려 만든 뗀석기' } ] }
  // img는 www/assets/items/<img>.png 가 실제로 있어야 한다

chart:{ type:'faces', title:'세 사람', people:[
  { img:'yisunsin', n:'이순신', s:'삼도수군통제사' } ] }
  // img는 www/assets/portraits/<img>.png 가 실제로 있어야 한다

chart:{ type:'quote', title:'삼국유사가 전하는 건국',
  text:'환웅이 무리 <b>삼천</b>을 이끌고…', from:'삼국유사 기이 고조선' }

chart:{ type:'stat', title:'고인돌의 크기', items:[
  { v:'53t', l:'강화 부근리 덮개돌' }, { v:'6.5m', l:'그 길이' } ] }

chart:{ type:'pyramid', title:'청동기 시대의 계급', tiers:[
  { t:'군장', s:'청동검을 찬다', hi:true }, { t:'백성' } ] }

chart:{ type:'vs', title:'제정일치', left:{ n:'단군', s:'제사' },
  right:{ n:'왕검', s:'정치' }, mid:'＝' }

chart:{ type:'map', title:'삼한의 자리', north:true, pins:[
  { x:38.6, y:121.6, n:'마한', on:true } ],
  legend:[ { k:'마한', v:'뒷날 백제' } ] }
  // 눈금: 가로 100 · 세로 150. 위경도 → x=(경도-124)/7*100, y=(43.3-위도)/9.5*150
  // north:true면 만주까지 보인다(한반도 밖 지명은 y가 음수)

chart:{ type:'steps', title:'청동검을 만드는 차례', steps:[
  { t:'거푸집을 빚는다', s:'흙이나 돌로' }, { t:'쇳물을 붓는다' } ] }

chart:{ type:'grid', title:'부여의 사출도', head:['갈래','가축'],
  rows:[ ['마가','말'], ['우가','소'] ], hi:0 }

// 예전부터 있던 것들: bar / flow / tree / kings / (type 없으면 표)
```

**하지 말 것**
- 번호 붙은 동그라미(문제 보기로 오해받는다 — 실제로 신고받아 고쳤다)
- 없는 그림 파일 가리키기 (`items/`·`portraits/`에 있는지 반드시 확인)
- 같은 도해를 연달아 두 번 (앞 것이 아직 유효하면 비워 둔다)

## 3. 고증 — 반드시 확인하고 쓴다

숫자·연도·인용문·지명을 쓸 때는 **검색해서 확인한 뒤** 쓴다. 기억으로 쓰지 않는다.

**믿을 곳** (이것만 쓴다)
- 우리역사넷 `contents.history.go.kr` (국사편찬위원회)
- 한국사데이터베이스 `db.history.go.kr`
- 한국민족문화대백과사전 `encykorea.aks.ac.kr`
- 국가유산포털 `heritage.go.kr`
- 국립중앙박물관 `museum.go.kr`

개인 블로그·카페·나무위키는 근거로 쓰지 않는다.

**민감한 시대**(일제강점기·현대)는 일방적 매도나 미화 없이 사실관계 중심으로 쓴다.

## 4. 손대지 말 것

- **`quizSeq`** — 문제와 해설은 그대로 둔다. `beats`만 바꾼다.
- **NPC 이름·초상·아이콘** — `name`, `img`, `icon` 그대로.
- **시험에 나오는 낱말** — 재미있게 쓴다고 '주먹도끼'를 '돌망치'로 바꾸면 안 된다.
  말투는 바꿔도 **용어는 그대로** 남긴다.

## 5. 내는 형식 — JSON

JS를 직접 쓰지 말고 JSON으로 낸다(문법 사고를 막는다).
`scripts/apply_beats.py`가 JSON을 읽어 챕터에 넣는다.

```json
{
  "chapter": "godae1.html",
  "npcs": {
    "geunchogo_0": [
      { "who": "npc", "t": "(지도를 펼치며) 자네, 이 강 이름을 아는가?" },
      { "who": "me",  "t": "…한강입니까?" },
      { "who": "npc", "t": "그렇지. 이 강을 쥔 자가 삼국을 쥔다네.",
        "chart": { "type": "map", "title": "한강을 두고 다툰 셋",
                   "pins": [ { "x": 44, "y": 92, "n": "한성", "on": true } ] } }
    ]
  }
}
```
